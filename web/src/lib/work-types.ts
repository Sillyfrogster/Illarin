import type { BrowseType } from "./api/query";

export const TYPE_LABELS: Record<BrowseType, string> = {
  character: "Character",
  lorebook: "Lorebook",
  preset: "Preset",
  theme: "Theme",
  pack: "Pack",
  extension: "Extension",
};
